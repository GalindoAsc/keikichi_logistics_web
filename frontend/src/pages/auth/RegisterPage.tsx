import RegisterForm from "../../components/auth/RegisterForm";

const RegisterPage = () => (
  <div className="space-y-4">
    <h2 className="text-xl font-semibold text-slate-800 text-center">Crear cuenta</h2>
    <RegisterForm />
  </div>
);

export default RegisterPage;
