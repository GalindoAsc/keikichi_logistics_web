# ==========================================
# KEIKICHI LOGISTICS - Frontend Development
# ==========================================

FROM node:20-alpine

WORKDIR /app

# Copiar package files
COPY package*.json ./

# Copiar archivos de configuración necesarios para Vite
COPY tsconfig.json tsconfig.node.json vite.config.ts tailwind.config.js postcss.config.js ./

# Instalar dependencias
RUN npm install

# Puerto para Vite dev server
EXPOSE 5173

# Comando para desarrollo con hot reload
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
